/**
 * The menu where users can create new events, tasks, and groups.
 * @component
 */

import styles from "./AddNewMenu.module.css";

interface AddNewMenuProps {
    
}

export function AddNewMenu({

}: AddNewMenuProps) {
    return (
        <div>
            
        </div>
    );
}