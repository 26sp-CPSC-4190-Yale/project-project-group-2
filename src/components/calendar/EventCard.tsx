/**
 * A generic card for an event in a calendar. Is selectable. Resizes based on the length of time of the event.
 * @component
 */

interface EventCardProps {
    
}

export function EventCard({

}: EventCardProps) {
    return (
        <div>
            
        </div>
    );
}