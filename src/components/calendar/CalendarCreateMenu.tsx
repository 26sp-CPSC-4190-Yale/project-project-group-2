/**
 * The menu where users can create a new calendar.
 * @component
 */

interface CalendarCreateMenuProps {
    
}

export function CalendarCreateMenu({

}: CalendarCreateMenuProps) {
    return (
        <div>
            
        </div>
    );
}