/**
 * The "plus" icon above the calendar to the right. Will show a hover effect. Is used to allow the user to open the menu to create a new event, task, or group.
 * @component
 */

import styles from "./AddNewButton.module.css";

interface AddNewButtonProps {
    
}

export function AddNewButton({

}: AddNewButtonProps) {
    return (
        <div>
            
        </div>
    );
}