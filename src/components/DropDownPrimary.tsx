/**
 * A generic component which contains both the closed and open versions of a dropdown menu.
 * @component
 */

interface DropDownPrimaryProps {
    
}

export function DropDownPrimary({

}: DropDownPrimaryProps) {
    return (
        <div>
            
        </div>
    );
}