/**
 * A basic component to put the logo in (which is currently just text).
 * @component
 */

interface LogoProps {
    
}

export function Logo({

}: LogoProps) {
    return (
        <div>
            
        </div>
    );
}